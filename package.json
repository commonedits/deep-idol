{
  "name": "DeepIdol",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "react-scripts": "0.9.5"
  },
  "dependencies": {
    "axios": "^0.15.3",
    "cloudinary": "^1.8.0",
    "cloudinary-react": "^1.0.2",
    "dotenv": "^4.0.0",
    "fs": "0.0.1-security",
    "history": "^4.6.0",
    "rc-upload": "^2.3.4",
    "react": "^15.4.2",
    "react-dom": "^15.4.2",
    "react-icons": "^2.2.3",
    "react-router": "^4.0.0",
    "react-router-dom": "^4.0.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "npm run build && cd build && aws s3 sync . s3://deepidol.com --exclude '.DS_Store'"
  }
}
